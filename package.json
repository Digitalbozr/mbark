{
  "name": "json-url-redirector",
  "version": "1.0.0",
  "description": "A simple URL redirect system backed by Firebase Firestore.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "firebase-admin": "^12.0.0",
    "dotenv": "^16.3.1"
  }
}
