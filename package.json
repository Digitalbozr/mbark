{
  "name": "json-url-redirector",
  "version": "1.0.0",
  "description": "A simple URL redirect system backed by a local JSON file.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
